{
  "include": [
    "packages",
    "apps",
    "internal",
    "scripts",
    "@types",
    "vitest.config.ts",
    "vitest.setup.ts",
    "oxlint.config.ts"
  ],
  "exclude": ["**/*.story.tsx", "**/*.story.ts"],
  "compilerOptions": {
    "types": ["node", "vitest/globals", "@testing-library/jest-dom"],
    "noEmit": true
  }
}
